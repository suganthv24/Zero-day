<!-- Announcements Page Content for main.hbs -->
<style>
  .announcements-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 40px 20px;
  }

  .announcements-header {
    text-align: center;
    margin-bottom: 50px;
  }

  .announcements-header h1 {
    font-size: 3rem;
    color: #333;
    font-style: italic;
    margin-bottom: 15px;
    font-family: 'Georgia', serif;
  }

  .announcements-header p {
    font-size: 1.2rem;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .create-announcement-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: #8B4513;
    color: white;
    border: none;
    padding: 15px 30px;
    border-radius: 8px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(139, 69, 19, 0.3);
    margin-bottom: 40px;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-family: 'Georgia', serif;
  }

  .create-announcement-btn:hover {
    background: #5d2d0c;
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(139, 69, 19, 0.4);
  }

  .announcements-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 30px;
    margin-bottom: 40px;
  }

  .announcement-card {
    background: white;
    border: 2px solid #333;
    border-radius: 12px;
    padding: 30px;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .announcement-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: #8B4513;
  }

  .announcement-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 20px;
  }

  .card-title {
    font-size: 1.5rem;
    font-weight: bold;
    color: #333;
    margin-bottom: 8px;
    font-style: italic;
    font-family: 'Georgia', serif;
  }

  .card-date {
    font-size: 0.9rem;
    color: #666;
    background: #f8f8f8;
    padding: 6px 12px;
    border-radius: 20px;
    white-space: nowrap;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .card-content {
    color: #555;
    line-height: 1.7;
    margin-bottom: 20px;
    font-family: 'Georgia', serif;
  }

  .card-priority {
    display: inline-block;
    padding: 8px 16px;
    border-radius: 6px;
    font-size: 0.8rem;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 1px;
    border: 2px solid;
  }

  .priority-high {
    background: #f8d7da;
    color: #721c24;
    border-color: #721c24;
  }

  .priority-medium {
    background: #fff3cd;
    color: #856404;
    border-color: #856404;
  }

  .priority-low {
    background: #d4edda;
    color: #155724;
    border-color: #155724;
  }

  .announcement-modal {
    display: none;
    position: fixed;
    z-index: 9999;
    left: 0;
    top: 0;
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.5);
    justify-content: center;
    align-items: center;
  }

  .announcement-modal-content {
    background: white;
    padding: 40px;
    border-radius: 12px;
    width: 90%;
    max-width: 500px;
    position: relative;
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
    border: 2px solid #333;
    transform: scale(0.9);
    animation: modalSlideIn 0.3s ease forwards;
  }

  @keyframes modalSlideIn {
    to { transform: scale(1); }
  }

  .announcement-close {
    position: absolute;
    top: 15px;
    right: 20px;
    font-size: 28px;
    font-weight: bold;
    color: #888;
    cursor: pointer;
    transition: color 0.3s;
  }

  .announcement-close:hover {
    color: #333;
  }

  .announcement-modal h2 {
    color: #333;
    margin-bottom: 30px;
    font-size: 1.8rem;
    font-style: italic;
    font-family: 'Georgia', serif;
    text-align: center;
  }

  .form-group {
    margin-bottom: 25px;
  }

  .form-group label {
    display: block;
    margin-bottom: 8px;
    color: #333;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 0.9rem;
  }

  .form-group input,
  .form-group textarea,
  .form-group select {
    width: 100%;
    padding: 12px 16px;
    border: 2px solid #ddd;
    border-radius: 6px;
    font-size: 1rem;
    transition: all 0.3s ease;
    font-family: 'Georgia', serif;
  }

  .form-group input:focus,
  .form-group textarea:focus,
  .form-group select:focus {
    outline: none;
    border-color: #8B4513;
    box-shadow: 0 0 0 3px rgba(139, 69, 19, 0.1);
  }

  .form-group textarea {
    height: 120px;
    resize: vertical;
  }

  .form-buttons {
    display: flex;
    gap: 15px;
    justify-content: center;
    margin-top: 30px;
  }

  .announcement-btn {
    padding: 12px 25px;
    border: none;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .btn-primary {
    background: #333;
    color: white;
  }

  .btn-primary:hover {
    background: #555;
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: #8B4513;
    color: white;
  }

  .btn-secondary:hover {
    background: #5d2d0c;
    transform: translateY(-2px);
  }

  .empty-state {
    text-align: center;
    padding: 80px 20px;
    color: #666;
    grid-column: 1 / -1;
  }

  .empty-state h3 {
    font-size: 1.8rem;
    margin-bottom: 15px;
    color: #333;
    font-style: italic;
    font-family: 'Georgia', serif;
  }

  .empty-state p {
    font-size: 1.1rem;
    color: #666;
  }

  @media (max-width: 768px) {
    .announcements-header h1 {
      font-size: 2.5rem;
    }
    
    .announcements-grid {
      grid-template-columns: 1fr;
    }
    
    .announcement-modal-content {
      padding: 30px 20px;
      margin: 20px;
    }

    .card-header {
      flex-direction: column;
      gap: 10px;
    }

    .form-buttons {
      flex-direction: column;
    }
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<div class="announcements-container">
  <div class="announcements-header">
    <h1>Announcements</h1>
    <p>Stay updated with our latest news & updates</p>
  </div>

  <div style="text-align: center; margin-bottom: 40px;">
    <button class="create-announcement-btn" onclick="openAnnouncementModal()">
      ➕ Create New Announcement
    </button>
  </div>

  <div class="announcements-grid" id="announcementsGrid">
    <!-- Sample announcements with restaurant theme -->
    <div class="announcement-card">
      <div class="card-header">
        <div>
          <div class="card-title">Welcome to RestroBytes!</div>
          <div class="card-date">Jan 15, 2025</div>
        </div>
      </div>
      <div class="card-content">
        Welcome to our authentic Italian dining experience! We're excited to serve you the finest pasta, pizza, and wine in a warm, traditional atmosphere. Buon appetito!
      </div>
      <div class="card-priority priority-high">High Priority</div>
    </div>

    <div class="announcement-card">
      <div class="card-header">
        <div>
          <div class="card-title">New Winter Menu Available</div>
          <div class="card-date">Jan 12, 2025</div>
        </div>
      </div>
      <div class="card-content">
        Our seasonal winter menu is now available featuring hearty risottos, warming soups, and traditional Italian comfort dishes. Perfect for the cold season!
      </div>
      <div class="card-priority priority-medium">Medium Priority</div>
    </div>

    <div class="announcement-card">
      <div class="card-header">
        <div>
          <div class="card-title">Wine Tasting Event</div>
          <div class="card-date">Jan 10, 2025</div>
        </div>
      </div>
      <div class="card-content">
        Join us for our monthly wine tasting event featuring selections from Tuscany and Piedmont. Every Friday at 7 PM. Reservations recommended.
      </div>
      <div class="card-priority priority-low">Low Priority</div>
    </div>
  </div>
</div>

<!-- Announcement Creation Modal -->
<div class="announcement-modal" id="announcementModal" onclick="closeAnnouncementModal(event)">
  <div class="announcement-modal-content" onclick="event.stopPropagation()">
    <span class="announcement-close" onclick="closeAnnouncementModal()">&times;</span>
    <h2>Create New Announcement</h2>
    <form id="announcementForm">
      <div class="form-group">
        <label for="announcementTitle">Announcement Title *</label>
        <input type="text" id="announcementTitle" name="title" required placeholder="Enter announcement title...">
      </div>

      <div class="form-group">
        <label for="announcementContent">Content *</label>
        <textarea id="announcementContent" name="content" placeholder="Enter your announcement details..." required></textarea>
      </div>

      <div class="form-group">
        <label for="announcementPriority">Priority Level</label>
        <select id="announcementPriority" name="priority">
          <option value="low">Low Priority</option>
          <option value="medium">Medium Priority</option>
          <option value="high">High Priority</option>
        </select>
      </div>

      <div class="form-buttons">
        <button type="button" class="announcement-btn btn-secondary" onclick="closeAnnouncementModal()">Cancel</button>
        <button type="submit" class="announcement-btn btn-primary">Create Announcement</button>
      </div>
    </form>
  </div>
</div>

<script>
  let restaurantAnnouncements = [
    {
      title: "Welcome to RestroBytes!",
      content: "Welcome to our authentic Italian dining experience! We're excited to serve you the finest pasta, pizza, and wine in a warm, traditional atmosphere. Buon appetito!",
      priority: "high",
      date: "Jan 15, 2025"
    },
    {
      title: "New Winter Menu Available",
      content: "Our seasonal winter menu is now available featuring hearty risottos, warming soups, and traditional Italian comfort dishes. Perfect for the cold season!",
      priority: "medium",
      date: "Jan 12, 2025"
    },
    {
      title: "Wine Tasting Event",
      content: "Join us for our monthly wine tasting event featuring selections from Tuscany and Piedmont. Every Friday at 7 PM. Reservations recommended.",
      priority: "low",
      date: "Jan 10, 2025"
    }
  ];

  function openAnnouncementModal() {
    document.getElementById('announcementModal').style.display = 'flex';
    document.body.style.overflow = 'hidden';
  }

  function closeAnnouncementModal(event) {
    if (event && event.target !== document.getElementById('announcementModal')) return;
    document.getElementById('announcementModal').style.display = 'none';
    document.body.style.overflow = 'auto';
    document.getElementById('announcementForm').reset();
  }

  function formatAnnouncementDate(date) {
    return date.toLocaleDateString('en-US', { 
      month: 'short', 
      day: 'numeric', 
      year: 'numeric' 
    });
  }

  function createAnnouncementCard(announcement) {
    const priorityClass = `priority-${announcement.priority}`;
    const priorityText = announcement.priority.charAt(0).toUpperCase() + announcement.priority.slice(1) + ' Priority';

    return `
      <div class="announcement-card" style="animation: slideInUp 0.5s ease;">
        <div class="card-header">
          <div>
            <div class="card-title">${announcement.title}</div>
            <div class="card-date">${announcement.date}</div>
          </div>
        </div>
        <div class="card-content">${announcement.content}</div>
        <div class="card-priority ${priorityClass}">${priorityText}</div>
      </div>
    `;
  }

  function renderRestaurantAnnouncements() {
    const grid = document.getElementById('announcementsGrid');
    
    if (restaurantAnnouncements.length === 0) {
      grid.innerHTML = `
        <div class="empty-state">
          <h3>No announcements yet</h3>
          <p>Create your first restaurant announcement to keep customers informed!</p>
        </div>
      `;
    } else {
      grid.innerHTML = restaurantAnnouncements.map(createAnnouncementCard).join('');
    }
  }

  // MODIFIED: This function now sends data to the server
  document.getElementById('announcementForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const formData = new FormData(e.target);
    const newAnnouncement = {
      title: formData.get('title'),
      content: formData.get('content'),
      priority: formData.get('priority'),
      date: formatAnnouncementDate(new Date())
    };

    // ADDED: Send data to server using fetch
    fetch('/admin/announcements', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        title: newAnnouncement.title,
        content: newAnnouncement.content,
        priority: newAnnouncement.priority
      })
    })
    .then(response => {
      if (response.ok) {
        // Keep the original UI behavior
        restaurantAnnouncements.unshift(newAnnouncement);
        renderRestaurantAnnouncements();
        closeAnnouncementModal();
        
        // Add success animation
        const grid = document.getElementById('announcementsGrid');
        const firstCard = grid.querySelector('.announcement-card');
        if (firstCard) {
          firstCard.style.animation = 'none';
          setTimeout(() => {
            firstCard.style.animation = 'slideInUp 0.5s ease';
          }, 10);
        }
        
        console.log('Announcement saved to database successfully!');
      } else {
        console.error('Error saving announcement to database');
        alert('Error saving announcement. Please try again.');
      }
    })
    .catch(error => {
      console.error('Network error:', error);
      alert('Network error. Please check your connection and try again.');
    });
  });

  // Close modal with Escape key
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      closeAnnouncementModal();
    }
  });

  // Initialize announcements
  document.addEventListener('DOMContentLoaded', function() {
    renderRestaurantAnnouncements();
  });
</script>